# 리센느 포카 교환기 --- 다음 수정 작업 지시서

> 로그인/PIN 문제는 해결 완료. 로그인 관련 수정은 이번 작업에서
> 제외한다.
>
> **작업 원칙:** 테스트·재시도·빌드·푸시 반복을 최소화하고 기능 구현을
> 우선한다. 기존 정상 작동 기능은 최대한 건드리지 않는다.

## 1. 수정 화면 --- 행사 / 발매 UI 정리

-   기존에 등록된 **행사와 발매를 선택할 수 있는 기능은 유지**한다.
-   행사 선택 칸과 발매 선택 칸의 크기를 줄인다.
-   **행사 / 발매를 한 줄에 2칸으로 나란히 배치**한다.
-   모바일 화면에서도 한 줄 배치가 깨지지 않도록 한다.

## 2. 수정 화면 --- 종류 선택 UI 정리

-   수정할 때 **기존에 등록되어 있는 종류를 선택할 수 있게** 한다.
-   필요한 경우 새로운 종류도 추가할 수 있게 한다.
-   종류 관련 선택/입력 UI도 공간을 줄여 **한 줄 배치**를 기본으로 한다.
-   기존 데이터와 호환되도록 한다.

## 3. 수정 화면 --- 도감 이름 초기화

-   기존 포카를 수정할 때 **도감 이름 입력칸은 기존 이름을 불러오지
    않는다.**
-   수정 화면 진입 시 도감 이름은 **빈칸(백지)** 상태로 시작한다.
-   행사 / 발매 / 종류 등 나머지 기존 정보는 정상적으로 불러온다.
-   사용자가 새 도감 이름을 입력한 뒤 저장하도록 한다.

## 4. 포카 도감 --- 행사별 그리드

-   전체 포카를 한 덩어리로 표시하지 않는다.

-   **행사별로 구분된 독립 그리드**로 표시한다.

-   예시:

    -   행사 A
        -   포카 / 포카 / 포카
    -   행사 B
        -   포카 / 포카 / 포카

-   새로운 행사가 추가되면 해당 행사 그리드가 자동으로 생성되게 한다.

-   기존 도감 검색/선택 기능은 유지한다.

## 5. 상단 사용자 UI 위치 변경

-   현재 **닉네임 + 로그아웃** 영역을 위쪽으로 이동한다.

-   화면 순서는 다음과 같이 한다.

    **닉네임 / 로그아웃**\
    ↓\
    **도감 \| 내 교환 \| 매칭 \| 거래내역**

-   기존 탭 기능 자체는 변경하지 않는다.

## 6. 교환방 --- 날짜 + 지역 선택

-   기존 날짜 선택 기능에 **지역 선택 기능**을 추가한다.
-   교환방을 **날짜 + 지역 조합**으로 구분한다.
-   같은 날짜이면서 같은 지역인 사용자끼리 교환/매칭 정보가 연결되도록
    한다.
-   지역은 고정값만 사용하지 않고:
    -   기존 지역 선택
    -   필요한 경우 새 지역 추가 가 가능하도록 구성한다.
-   GPS/실시간 위치 기능은 사용하지 않는다.

## 7. 교환 약속 --- 시간 연장 기능

-   교환 상태는 기존 계획대로:

    **교환 가능 → 약속 중 → 완료**

-   `약속 중` 상태에서 **남은 시간을 표시**한다.

-   **시간 연장 버튼**을 추가한다.

-   기본안:

    -   최초 약속 시간: 15분
    -   연장: +15분
    -   연장 가능 횟수: 1회

-   시간이 만료되면 자동으로 `교환 가능` 상태로 복귀한다.

-   `교환 완료`를 누르면 즉시 완료 처리한다.

-   동일 포카를 여러 장 등록한 경우 완료할 때마다 **수량을 자동으로 1장
    차감**한다.

-   수량이 0이 되면 해당 교환 등록을 종료한다.

## 8. 맞교환 매칭 알림

-   새로운 **💕 맞교환**이 성립하면 사용자에게 알림을 표시한다.
-   우선 구현:
    -   사이트 내부 팝업 또는 배너
    -   가능하면 진동
-   알림을 누르면 해당 매칭 화면으로 바로 이동한다.
-   **동일한 매칭을 반복해서 알리지 않는다.**
-   신규 매칭이 발생했을 때만 1회 알림 처리한다.
-   브라우저 푸시 알림은 이번 핵심 작업이 아니다. 기본 기능 완료 후
    필요하면 별도 추가한다.

------------------------------------------------------------------------

## 기존 확정 기능 --- 유지할 것

아래 기능은 삭제하거나 구조를 깨지 않는다.

-   서비스 이름: **리센느 포카 교환기**

-   공용 포카 도감

-   도감에서 포카 선택 후 내 교환 등록

-   포카 사진/정보 누적 저장

-   같은 포카 여러 장 등록 및 수량 관리

-   원하는 포카 복수 선택

-   **아무거나 가능** 선택

-   서로 조건이 맞으면 **💕 맞교환 최우선 표시**

-   멤버/유닛은 직접 이름을 입력하는 방식이 아니라 **버튼식 선택**

    -   원이
    -   리브
    -   미나미
    -   메이
    -   제나
    -   단체

-   개인 포카: 멤버 1명 선택

-   유닛 포카: 멤버 2명 이상 복수 선택

-   단체 포카: 단체 선택

-   거래내역 저장

-   「여기 있어요」 화면:

    -   검은 배경
    -   포카 사진 1장 크게
    -   닉네임 분홍색 + 큰 글씨
    -   교환번호 표시

-   로그인 화면 안내문:

    **금전 거래 및 되팔이를 금합니다.**\
    **리센느에게 부끄럽지 않은 덕질을 합시다.**\
    **모두 즐거운 포카 모으기 되세요! 💗**

-   택배/판매/금전거래 기능은 만들지 않는다.

------------------------------------------------------------------------

## 구현 우선순위

**1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 순서로 진행한다.**

특히 이번 작업에서는 **불필요한 테스트 반복, 로그인 재검증, 중간 단계별
푸시를 최소화**한다.\
가능한 기능 수정사항을 먼저 구현한 뒤 필요한 핵심 테스트를 마지막에
묶어서 진행한다.
